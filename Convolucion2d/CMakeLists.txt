cmake_minimum_required(VERSION 2.4.10)
project( Convolucion )
find_package( OpenCV REQUIRED )
find_package( CUDA REQUIRED )
set(CUDA_NVCC_FLAGS ${CUDA_NVCC_FLAGS}; -O3; -arch=compute_35)
cuda_add_executable( convolucion2d convolucion2d.cu )
target_link_libraries( convolucion2d ${OpenCV_LIBS} )
